import { requestId, requireAdmin, safeJson, sanitizeText } from "../../../../lib/http/api";
export async function POST(request: Request) { const unauthorized = requireAdmin(request); if (unauthorized) return unauthorized; const body = await safeJson<{ deviceId?: string; fieldPath?: string; status?: string; sourceId?: string; note?: string }>(request); if (!body?.deviceId || !body.fieldPath || !body.sourceId || !["verified", "conflicting", "unverified"].includes(body.status ?? "")) return Response.json({ error: "deviceId, fieldPath, sourceId and a valid status are required" }, { status: 400 }); return Response.json({ data: { id: crypto.randomUUID(), ...body, note: sanitizeText(body.note, 500), verifiedAt: new Date().toISOString() }, meta: { requestId: requestId(request) } }, { status: 201 }); }
