# Planned database schema

The Prisma schema under `prisma/` (and the copy here) describes a future PostgreSQL model.

The live site currently uses the JSON analysis cache (`lib/data/devices.json`) plus the live specs API. Prisma is not required to run the app today.
